using
(
    class(list)
    (
        each:list.each,
        add:element -> class(
            ((l, e) -> each:a -> [l.each(a), a(e)])
            (this, element)
        )
    )
)
as(List) List([1])
    .add(2)
    .add(3)
    .add(11)
    .add(55)
    .each(n -> [print(n), print(" ")])
    